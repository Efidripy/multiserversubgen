# 🚀 node panel Unified Subscription Manager

Легкое и мощное решение на Python (FastAPI) для объединения подписок из нескольких панелей node panel в одну универсальную ссылку. 



## 🌟 Основные возможности

* **Агрегация узлов**: Собирает VLESS+Reality конфигурации с неограниченного количества серверов node panel.
* **Автоматизация Reality**: Интеллектуальный подхват настроек (Public Key, ShortID, SNI, Flow) и автоматическая подстановка порта 443.
* **Админ-панель**: Удобный интерфейс для управления узлами (добавление/удаление/проверка статуса).
* **PAM Авторизация**: Вход в админку защищен системными учетными записями Linux.
* **Трекинг статистики**: Учет количества скачиваний и времени последней активности для каждого пользователя (Email).
* **Формат Sub**: Генерация чистого Base64, который понимают все современные клиенты: v2rayN, Shadowrocket, Streisand, V2Box, Nekoray.

## 🛠 Быстрый старт

1. Подготовка системы
Убедитесь, что у вас установлен Python 3.10+ и необходимые библиотеки для работы PAM:
bash
sudo apt update && sudo apt install python3-pip python3-venv libpam0g-dev -y

2. Установка
Bash
git clone [https://github.com/yourusername/node panel-sub-manager.git](https://github.com/yourusername/node panel-sub-manager.git)
cd node panel-sub-manager
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
3. Настройка окружения
Проект использует папку templates для хранения HTML-интерфейса. Убедитесь, что структура выглядит так:

Plaintext
.
├── main.py
├── admin.db          # Создастся автоматически
└── templates/
    └── index.html    # Файл интерфейса
⚙️ Деплой через Nginx
Для работы через подпапку (например, https://domain.com/sub-page/) используйте следующую конфигурацию:

Nginx
location /sub-page/ {
    proxy_pass [http://127.0.0.1:666/](http://127.0.0.1:666/); # Конечный слэш важен!
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_intercept_errors off;
    proxy_buffering off;
}
🖥 Автозапуск (Systemd)
Создайте файл /etc/systemd/system/sub-manager.service:

Ini, TOML
[Unit]
Description=node panel Subscription Manager
After=network.target

[Service]
User=root
WorkingDirectory=/opt/node panel-sub-manager
ExecStart=/opt/node panel-sub-manager/venv/bin/uvicorn main:app --host 127.0.0.1 --port 666
Restart=always

[Install]
WantedBy=multi-user.target
📊 Использование
Зайдите в админку: https://your-domain.com/sub-page/.

Авторизуйтесь под своим Linux-пользователем (например, root).

Добавьте URL ваших панелей node panel и учетные данные API.

Раздайте пользователям ссылки вида: https://your-domain.com/sub-page/sub/user@email.com.

📜 Лицензия
Распространяется под лицензией MIT. Свободно для личного и коммерческого использования.
