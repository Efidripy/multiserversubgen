Local MCP tooling for the `multiserversubgen` workspace.

This folder keeps MCP dependencies local to the repo and provides stable
Windows wrapper scripts under `bin/`.

Fill environment variables from `.env.example` before using servers that need
credentials or runtime configuration.
